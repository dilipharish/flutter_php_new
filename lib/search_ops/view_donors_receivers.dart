import 'package:flutter/material.dart';

class ViewDonorsReceiversPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Donors/Receivers'),
      ),
      body: Center(
        child: Text('View Donors/Receivers Page Content'),
      ),
    );
  }
}
